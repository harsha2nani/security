server:
  port: 8080
spring:
  application:
    name: security-service
  dataSource:
    url: jdbc:mysql://localhost:3306/security
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update